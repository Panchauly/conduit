[package]
name = "grpc-producer-example"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
conduit-client = { path = "../../../crates/conduit-client" }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
