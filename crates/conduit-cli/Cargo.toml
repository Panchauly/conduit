[package]
name = "conduit-cli"
version = "0.5.0-alpha"
edition = "2024"

[dependencies]
conduit-core = { path = "../conduit-core" }
conduit-ingest = { path = "../conduit-ingest" }
clap = { version = "4", features = ["derive"] }
serde_json = "1.0"
ctrlc = "3"
